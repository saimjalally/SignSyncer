let supabase=null,session=null,profile=null;const injectedConfig=window.__SUPABASE_CONFIG__||{};const cfg={url:injectedConfig.url||'',key:injectedConfig.key||''};async function init(){if(cfg.url&&cfg.key){const{createClient}=await import('https://esm.sh/@supabase/supabase-js@2');supabase=createClient(cfg.url,cfg.key);session=(await supabase.auth.getSession()).data.session;supabase.auth.onAuthStateChange((_,s)=>{session=s;route(location.pathname)});if(session)profile=await getProfile()}route(location.pathname)}
async function getProfile(){let{data,error}=await supabase.from('profiles').select('*').eq('id',session.user.id).single();if(error&&error.code==='PGRST116'){await supabase.from('profiles').insert({id:session.user.id,full_name:session.user.user_metadata?.full_name||'',role:'user'});return getProfile()}return data}function go(p){history.pushState(null,'',p);route(p);scrollTo(0,0)}addEventListener('popstate',()=>route(location.pathname));
function shell(content){document.title='SignSyncer';app.innerHTML=`<header><a class=brand>◆ SignSyncer</a><button class=mobile>Menu</button><nav>${['Features','Pricing','Security'].map(x=>`<a data-go="/${x.toLowerCase()}">${x}</a>`).join('')}<a data-go="/support">Support</a>${session?`<a data-go="/dashboard">Dashboard</a><button id=logout>Logout</button>`:`<button data-go="/login">Log in</button><button class=primary data-go="/signup">Start free</button>`}</nav></header>${!supabase?`<div class=setup>⚠️ Configure Supabase by setting VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in Vercel. Backend actions are disabled until configured.</div>`:''}<main>${content}</main><footer><b>SignSyncer</b><a data-go="/privacy">Privacy</a><a data-go="/terms">Terms</a><a data-go="/contact">Contact</a></footer>`;bindNav();document.querySelector('.brand').onclick=()=>go('/');document.querySelector('.mobile').onclick=()=>document.querySelector('nav').classList.toggle('show');let lo=document.getElementById('logout');if(lo)lo.onclick=async()=>{await supabase.auth.signOut();session=null;profile=null;go('/')}}function bindNav(){document.querySelectorAll('[data-go]').forEach(e=>e.onclick=()=>go(e.dataset.go))}function page(t,b){return `<section><h1>${t}</h1><p>${b}</p></section>`}function protectedPage(fn){if(!session)return auth('login',location.pathname);return fn()}function route(p){if(p.startsWith('/support/ticket/'))return protectedPage(()=>ticket(p.split('/').pop()));({ '/':home,'/features':()=>marketing('Features'),'/pricing':pricing,'/security':()=>page('Security','Supabase Auth, Postgres row-level security policies, protected routes and server-side authorization rules are documented for production deployment.'),'/about':()=>page('About SignSyncer','SignSyncer helps teams manage consistent email signature operations.'),'/contact':contact,'/privacy':()=>page('Privacy Policy','Operational privacy policy structure is provided. Have counsel review final legal text before launch.'),'/terms':()=>page('Terms of Service','Operational terms structure is provided. Have counsel review final legal text before launch.'),'/login':()=>auth('login'),'/signup':()=>auth('signup'),'/forgot-password':forgot,'/reset-password':reset,'/verify-email':()=>page('Email verification','If you opened this from Supabase email verification, your account state is updated by Supabase Auth.'),'/dashboard':()=>protectedPage(dashboard),'/profile':()=>protectedPage(profilePage),'/settings':()=>protectedPage(settings),'/notifications':()=>protectedPage(notifications),'/support':()=>protectedPage(support),'/support/new':()=>protectedPage(newTicket),'/admin':()=>protectedPage(admin),'/unauthorized':()=>page('Access denied','You do not have permission to open this page.')}[p]||notFound)()}function home(){shell(`<section class=hero><p class=eyebrow>Production SaaS signature operations</p><h1>Synchronize every email signature without sacrificing security.</h1><p>Manage account profiles, support requests and team-ready signature workflows from one secure portal.</p><button class=primary data-go="${session?'/dashboard':'/signup'}">Get started</button><button data-go="/features">Explore features</button></section>${cards(['Secure authentication','Profile management','Support ticketing','Notifications','Role-aware admin','Vercel-ready routing'])}`);bindNav()}function marketing(t){shell(`<section><h1>${t}</h1>${cards(['Email signature governance','Customer support workflows','Responsive dashboards','Accessible forms','SEO public pages','Secure data access'])}</section>`)}function cards(items){return`<div class=grid>${items.map(i=>`<article class=card><h3>✓ ${i}</h3><p>Real application flow designed for Supabase persistence and production RLS.</p></article>`).join('')}</div>`}function pricing(){shell(`<section><h1>Simple pricing</h1><div class=grid><article class=card><h3>Starter</h3><b>$0</b><p>Evaluate securely.</p><button data-go=/signup>Create account</button></article><article class="card featured"><h3>Business</h3><b>$12/user</b><p>Team workflows and support operations.</p><button data-go=/contact>Contact sales</button></article></div></section>`);bindNav()}function form(title,body,msg=''){shell(`<section><form class="card form"><h1>${title}</h1>${body}${msg?`<p class=msg>${msg}</p>`:''}</form></section>`);bindNav()}function input(n,t='text'){return`<label>${n}<input required name="${n.toLowerCase().replaceAll(' ','_')}" type=${t}></label>`}function auth(mode,redirect='/dashboard'){form(mode==='signup'?'Create account':'Welcome back',`${mode==='signup'?input('Name'):''}${input('Email','email')}${input('Password','password')}${mode==='signup'?input('Confirm password','password'):''}<button class=primary>${mode==='signup'?'Sign up':'Log in'}</button><a data-go=/forgot-password>Forgot password?</a>`);document.querySelector('form').onsubmit=async e=>{e.preventDefault();let f=Object.fromEntries(new FormData(e.target));if(!supabase)return alert('Supabase is not configured.');if(mode==='signup'){if(f.password.length<12)return alert('Password must be at least 12 characters.');if(f.password!==f.confirm_password)return alert('Passwords do not match.');let{error}=await supabase.auth.signUp({email:f.email,password:f.password,options:{data:{full_name:f.name},emailRedirectTo:location.origin+'/verify-email'}});alert(error?error.message:'Check your email to verify your account.')}else{let{error}=await supabase.auth.signInWithPassword({email:f.email,password:f.password});error?alert(error.message):go(redirect)}}}function forgot(){form('Reset password',`${input('Email','email')}<button class=primary>Send reset link</button>`);document.querySelector('form').onsubmit=async e=>{e.preventDefault();if(!supabase)return alert('Supabase is not configured.');let email=new FormData(e.target).get('email');let{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:location.origin+'/reset-password'});alert(error?error.message:'Password reset email sent.')}}function reset(){form('Choose a new password',`${input('New password','password')}<button class=primary>Update password</button>`);document.querySelector('form').onsubmit=async e=>{e.preventDefault();let password=new FormData(e.target).get('new_password');let{error}=await supabase.auth.updateUser({password});alert(error?error.message:'Password updated.')}}function dashShell(content){shell(`<div class=dash><aside>${[['/dashboard','Dashboard'],['/profile','Profile'],['/settings','Settings'],['/support','Support'],['/notifications','Notifications'],['/admin','Admin']].map(x=>`<a data-go="${x[0]}">${x[1]}</a>`).join('')}</aside><section>${content}</section></div>`);bindNav()}function dashboard(){dashShell(`<h1>Dashboard</h1><div class=grid>${['Email:'+session.user.email,'Role:'+(profile?.role||'user'),'Account:Active'].map(s=>`<article class=card><b>${s.split(':')[0]}</b><p>${s.split(':')[1]}</p></article>`).join('')}</div>`)}function profilePage(){dashShell(`<h1>Profile</h1><div class="card form"><label>Full name<input id=name value="${profile?.full_name||''}"></label><button class=primary id=save>Save changes</button></div>`);save.onclick=async()=>{let{error}=await supabase.from('profiles').update({full_name:name.value,updated_at:new Date().toISOString()}).eq('id',session.user.id);alert(error?error.message:'Profile saved.');profile=await getProfile()}}function settings(){dashShell(`<h1>Settings</h1>${cards(['General preferences','Security password reset','Notification controls','Account data management'])}`)}async function notifications(){let rows=(await supabase.from('notifications').select('*').order('created_at',{ascending:false})).data||[];dashShell(`<h1>Notifications</h1>${rows.length?cards(rows.map(r=>r.title)):'<p class=empty>No notifications yet.</p>'}`)}async function support(){let rows=(await supabase.from('support_tickets').select('*').order('created_at',{ascending:false})).data||[];dashShell(`<h1>Support tickets</h1><button class=primary data-go=/support/new>New ticket</button><div class=grid>${rows.map(t=>`<article class=card><h3>${t.subject}</h3><p>${t.category} · ${t.priority} · ${t.status}</p><button data-go="/support/ticket/${t.id}">Open</button></article>`).join('')||'<p class=empty>No support tickets yet.</p>'}</div>`);bindNav()}function newTicket(){form('Create support ticket',`${input('Subject')}<label>Description<textarea name=description required minlength=10></textarea></label><label>Category<select name=category>${['Technical Issue','Account','Billing','Feature Request','Bug Report','General Question'].map(x=>`<option>${x}</option>`)}</select></label><label>Priority<select name=priority>${['Low','Normal','High','Urgent'].map(x=>`<option>${x}</option>`)}</select></label><button class=primary>Submit ticket</button>`);document.querySelector('form').onsubmit=async e=>{e.preventDefault();let f=Object.fromEntries(new FormData(e.target));let{data,error}=await supabase.from('support_tickets').insert({...f,user_id:session.user.id,status:'Open'}).select().single();error?alert(error.message):go('/support/ticket/'+data.id)}}async function ticket(id){let t=(await supabase.from('support_tickets').select('*').eq('id',id).single()).data;if(!t)return page('Ticket not found','The requested support ticket is unavailable.');let rows=(await supabase.from('ticket_messages').select('*').eq('ticket_id',id).order('created_at')).data||[];dashShell(`<h1>${t.subject}</h1><p>${t.category} · ${t.priority} · ${t.status}</p>${rows.map(m=>`<article class=card><p>${m.body}</p><small>${new Date(m.created_at).toLocaleString()}</small></article>`).join('')}${['Closed','Resolved'].includes(t.status)?'<p>This ticket is closed.</p>':`<div class=card><textarea id=reply placeholder=Reply></textarea><button class=primary id=send>Send reply</button></div>`}`);if(send)send.onclick=async()=>{let{error}=await supabase.from('ticket_messages').insert({ticket_id:id,sender_id:session.user.id,body:reply.value});error?alert(error.message):go('/support/ticket/'+id)}}async function admin(){if(!['admin','support_agent'].includes(profile?.role))return page('Access denied','Admin access requires a support_agent or admin role and is enforced by RLS.');let rows=(await supabase.from('support_tickets').select('*')).data||[];dashShell(`<h1>Support admin</h1>${cards(rows.map(r=>r.subject))}`)}function contact(){form('Contact us',`${input('Email','email')}<label>Message<textarea required></textarea></label><button class=primary>Send</button><p>For production, connect contact routing to your support inbox or database workflow.</p>`)}function notFound(){shell(`<section><h1>Page not found</h1><button data-go=/>Go home</button></section>`);bindNav()}init();
